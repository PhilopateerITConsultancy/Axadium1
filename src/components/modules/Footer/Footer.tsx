import { Box } from '@chakra-ui/react';

const Footer = () => {
  return (
    <Box textAlign={'center'} w="full" p={6}>
      {/* Footer is intentionally left minimal. Add your own branding or links here if needed. */}
    </Box>
  );
};

export default Footer;
