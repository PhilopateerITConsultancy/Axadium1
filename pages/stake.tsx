import { Default } from 'components/layouts/Default';
import { Home } from 'components/templates/home';
import type { NextPage } from 'next';


export default function StakePage() {
  return (
    <Default>
      <h1>Stake Page</h1>
      <p>This is the staking interface.</p>
    </Default>
  );
}